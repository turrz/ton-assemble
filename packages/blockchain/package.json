{
  "name": "@ton-site-builder/blockchain",
  "version": "1.0.0",
  "description": "TON blockchain client helpers for TonAssemble.",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "npx tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "lint": "echo \"no linter configured for blockchain\""
  },
  "dependencies": {
    "@ton/core": "^0.63.1",
    "@ton/ton": "^15.0.0",
    "zod": "^3.23.8"
  }
}
